# /prototypes — Disposable Experiments

Throwaway code supporting spikes (#8–#13). Nothing here is production code, nothing here is a dependency of `/plugins`, and anything worth keeping graduates into a plugin through a reviewed PR. Same safety rules as everywhere: no real data, no unlicensed content, no secrets.
