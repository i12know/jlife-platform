{
    "name": "i12know/jlife-huddles",
    "description": "Test dependencies for the jlife-huddles plugin. Installed per-plugin so vendor/ is inside the wp-env plugin mapping and phpunit is runnable from the tests-cli container.",
    "license": "GPL-2.0-or-later",
    "require-dev": {
        "phpunit/phpunit": "^9.6",
        "yoast/phpunit-polyfills": "^2.0"
    },
    "scripts": {
        "test": "phpunit"
    }
}
